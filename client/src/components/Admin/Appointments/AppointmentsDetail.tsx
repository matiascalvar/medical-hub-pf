import { useState, useEffect } from "react";
import { getPatientDetails, updatePatient  } from "../requests";

export default function AppointmentsDetail(props: any) : JSX.Element {
    return (
        <div>APPOINTMENTS DETAIL</div>
    )
}